- rule: Ads and Sponsors
  match: title
  regex: \bad\b|advert(isement)?|sponsor(ed)?

- rule: Rule that applies to a specific feed only
  host: somesite.com
  match: all
  regex: flamboyant|discombobulate
