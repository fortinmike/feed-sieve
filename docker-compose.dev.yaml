services:
  feed-sieve-dev:
    container_name: feed-sieve-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - 9011:5000
    volumes:
      - ./:/app:cached
    restart: unless-stopped
